name = "shaw-availability-cron"
main = "src/index.js"
compatibility_date = "2026-07-21"

[triggers]
# Hourly, 7am-11pm Singapore time (UTC+8, no DST). SGT 07:00-23:00 is
# UTC 23:00 (previous day) through UTC 15:00.
crons = ["0 0-15,23 * * *"]

[vars]
GITHUB_OWNER = "shubham93g"
GITHUB_REPO = "shaw-availability"
GITHUB_WORKFLOW_FILE = "scan.yml"
GITHUB_REF = "main"

# GITHUB_TOKEN is a secret, not a var - set it with:
#   wrangler secret put GITHUB_TOKEN
